#pragma once

class Color;
struct DrawModelState_t;
struct ModelRenderInfo_t;
struct Matrix3x4;
class Player_t;

class Chams final
{
	/*void overrideChams(bool ignore, bool wireframe, const Color& color);
	void run(void* ctx, const DrawModelState_t& state, const ModelRenderInfo_t& info, Matrix3x4* matrix);
	void drawChams(Player_t* ent, void* ctx, const DrawModelState_t& state, const ModelRenderInfo_t& info, Matrix3x4* matrix);
	void drawBacktrackChams(Player_t* ent, void* ctx, const DrawModelState_t& state, const ModelRenderInfo_t& info);
	void drawModel(void* ctx, const DrawModelState_t& state, const ModelRenderInfo_t& info, Matrix3x4* matrix);*/
};

inline Chams chams;